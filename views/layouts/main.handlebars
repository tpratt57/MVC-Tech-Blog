<!DOCTYPE html>
<html lang="en">

<head>
    <meta charset="UTF-8">
    <meta name="viewport" content="width=device-width, initial-scale = 1.0">
    <title></title>
    <link rel="stylesheet" href="/public/css/style.css">
</head>

<div class="wrapper">
    <header>
        <h1 class="has-text-black px-3 py-3 has-text-weight-bold"></h1>
        <nav class="navbar mt-2">
            {{#if loggedIn}}
            <a href="/" class="navbar-item is-hoverable pt-1">Home Page</a>
            <a href="/dashboard" class="navbar-item is-hoverable pt-1 mr-4">Create Post</a>
            <button id="logout" class="button is-danger">Log Out</button>
            {{else}}
            <a href="/" class="navbar-item is-hoverable pt-1">Home Page</a>
            <a href="/login" class="button is-primary">Log In</a>
            {{/if}}
        </nav>
    </header>

    <main>
        {{{body}}}
    </main>

    <footer class="footer rows pb-2">
        <div class="row-is-full content has-text centered">
            <p>
                <a href="/">Home Page</a>{{#if loggedIn}} |
                <a href="/dashboard">Dashboard</a> |
                <a href="/dashboard/create/">Create New Post</a>
                {{else}}
                | <a href="/login/">Log In</a>
                {{/if}}
            </p>
        </div>
        <div class="row-is-full content has-text-centered">
            <p>
                <strong>

                </strong>
            </p>
        </div>
    </footer>
</div>

{{#if loggedIn}}
<script src="/public/js/logout.js"></script>
{{/if}}
</body>

</html>